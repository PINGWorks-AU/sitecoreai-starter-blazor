@inherits SitecoreComponent<PartialDesignDynamicPlaceholder.ViewModel>

<ScPlaceholder Name="@Model.Sig" />

@code {
	public sealed class ViewModel : ViewModelBase
	{
		[SitecoreComponentParam]
		public string? Sig { get; set; }
	}

	protected override void OnInitialized()
	{
		base.OnInitialized();

		var a = Model.Sig;
	}
}
